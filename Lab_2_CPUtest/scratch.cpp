/*/	Multiplication/division
	loop_count = 0;
	ops = 0;
	while (clock = < 5)
	{
		clock = // clock function
			while (ops<40000)
		{
			temp_mul = short_int * short_int;
			temp_div = short_int / short_int;
			ops = ops +1;
		}
		
		loop_count = loop_count + 1;
	}

	//	Mixed operations
	loop_count = 0;
	ops = 0;
	while (clock = < 5)
	{
		clock = // clock function
				while (ops<40000)
		{
			temp_add = short_int + short_int;
			temp_sub = short_int - short_int;
			temp_mul = short_int * short_int;
			temp_div = short_int / short_int;
			ops = ops +1;
		}
		
		
		loop_count = loop_count + 1;
	}
	*/